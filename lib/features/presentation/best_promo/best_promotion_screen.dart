import 'package:flutter/material.dart';

class BestPromotionsScreen extends StatelessWidget {
  const BestPromotionsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Best Promotions'),
      ),
    );
  }
}
